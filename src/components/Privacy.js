const Privacy_div = ({ children, className = "" }) => {
    return (
        <div className={`font-playfair mb-4 ${className}`}>
            {children}
        </div>
    );
};

export default Privacy_div;